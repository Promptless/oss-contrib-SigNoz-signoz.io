---
date: 2026-02-19
title: Send logs from Logstash to SigNoz
description: Send logs from Logstash to SigNoz using OpenTelemetry Collector.
doc_type: howto
id: logstash_to_signoz
---

import GetHelp from '@/components/shared/get-help.md'

If you use Logstash to collect logs in your stack, you can forward them to SigNoz via the OpenTelemetry Collector using the TCP protocol.

## Prerequisites

- A running Logstash instance
- An instance of SigNoz (either [Cloud](https://signoz.io/teams/) or [Self-Hosted](https://signoz.io/docs/install/self-host/))

<Tabs entityName="plans">
<TabItem value="signoz-cloud" label="SigNoz Cloud" default>

### Send Logs Using Logstash in SigNoz Cloud

#### Step 1: Add OpenTelemetry Collector Binary

Add the OpenTelemetry Collector binary to your VM by following the [OTel binary setup guide](https://signoz.io/docs/tutorial/opentelemetry-binary-usage-in-virtual-machine/).

#### Step 2: Configure the `tcplog` Receiver

Add the `tcplog` receiver to your `config.yaml`:

```yaml
receivers:
  tcplog/logstash:
    max_log_size: 1MiB
    listen_address: '0.0.0.0:2256'
    attributes: {}
    resource: {}
    add_attributes: false
    operators: []
```

- Port `2256` is used here, but you can use any available port.
- Learn more about the <a href="https://github.com/open-telemetry/opentelemetry-collector-contrib/tree/main/receiver/tcplogreceiver" target="_blank" rel="noopener noreferrer nofollow">tcplog receiver</a>.

#### Step 3: Enable the Receiver in the Logs Pipeline

Update your `config.yaml` service configuration to include the receiver:

```yaml {4}
service:
    ...
    logs:
        receivers: [otlp, tcplog/logstash]
        processors: [batch]
        exporters: [otlp]
```

#### Step 4: Update Logstash Configuration

Change the Logstash output config to forward logs to the OpenTelemetry Collector:

```ruby
output {
  tcp {
    codec => json_lines # Ensures logs are sent in JSON format line-by-line
    host => "localhost"
    port => 2256
  }
}
```

- This config assumes Logstash is running on the same host as the Collector. Adjust the `host` value if Logstash is running elsewhere.

#### Step 5: Start the Services

Restart the OpenTelemetry Collector and Logstash.

</TabItem>

<TabItem value='self-host' label='Self-Host'>

### Send Logs Using Logstash in Self-Hosted SigNoz

#### Step 1: Configure the `tcplog` Receiver

Add the receiver to your `otel-collector-config.yaml` located in `deploy/docker`:

```yaml
receivers:
  tcplog/logstash:
    max_log_size: 1MiB
    listen_address: '0.0.0.0:2256'
    attributes: {}
    resource: {}
    add_attributes: false
    operators: []
```

- Port `2256` is used here, but you can use any available port.
- Learn more about the <a href="https://github.com/open-telemetry/opentelemetry-collector-contrib/tree/main/receiver/tcplogreceiver" target="_blank" rel="noopener noreferrer nofollow">tcplog receiver</a>.

#### Step 2: Enable the Receiver in the Logs Pipeline

Update the `otel-collector-config.yaml` to include the receiver in the logs pipeline:

```yaml {4}
service:
  ...

  logs:
    receivers: [otlp, tcplog/logstash]
    processors: [batch]
    exporters: [clickhouselogsexporter]
```

#### Step 3: Expose the Port

Update the `docker-compose.yaml` file in `deploy/docker` to expose the port:

```yaml
otel-collector:
  ...
  ports:
    - "2256:2256"
```

#### Step 4: Update Logstash Configuration

Modify the Logstash output config to forward logs to the OpenTelemetry Collector:

```ruby
output {
  tcp {
    codec => json_lines # Ensures logs are sent in JSON format line-by-line
    host => "<otel-collector-host>"
    port => 2256
  }
}
```

- `<otel-collector-host>`: The host where the OpenTelemetry Collector is running. Refer to the [troubleshooting guide](https://signoz.io/docs/install/troubleshooting/#signoz-otel-collector-address-grid) for details.

#### Step 5: Start the Services

Restart Logstash and SigNoz.

</TabItem>

</Tabs>

## Validate

Once the services are running:

1. Open SigNoz and navigate to **Logs** > **Logs Explorer**.
2. You should see your Logstash logs appearing within a few seconds.
3. Verify the log entries contain the expected fields from your Logstash pipeline.

## Transform Logs (Optional)

Use OpenTelemetry processors to transform your log model into the <a href="https://github.com/open-telemetry/opentelemetry-specification/blob/main/specification/logs/data-model.md" target="_blank" rel="noopener noreferrer nofollow">OpenTelemetry log data model</a>. Learn more about [available log processors](https://signoz.io/docs/logs-pipelines/processors/).

<details>
<ToggleHeading>

## Troubleshooting

</ToggleHeading>

#### Logs are not appearing in SigNoz

- Verify that the OpenTelemetry Collector is running and the `tcplog/logstash` receiver is enabled in both the `receivers` section and the `service.pipelines.logs` pipeline.
- Confirm that Logstash is sending logs to the correct host and port (e.g., `localhost:2256`).
- Check the OpenTelemetry Collector logs for errors related to the `tcplog` receiver.

#### Port conflict

- If port `2256` is already in use, choose a different port. Update the port in both the Collector `config.yaml` and the Logstash output configuration.

#### Logs appear but are not parsed correctly

- Ensure the Logstash `output` block uses `codec => json_lines` so logs are sent in JSON format line-by-line.
- Use [Log Pipelines](https://signoz.io/docs/logs-pipelines/introduction/) in SigNoz to parse and transform log fields.

</details>

## Next Steps

- [Log query and filtering](https://signoz.io/docs/logs-management/overview/)
- [Set up alerts on logs](https://signoz.io/docs/alerts-management/log-based-alerts/)
- [Log Pipelines for preprocessing](https://signoz.io/docs/logs-pipelines/introduction/)

## Get Help

<GetHelp />
